#include<stdio.h>
/**
 *main - Entry point
 *Return: End of program
 *(Program by Hayzed)
 */
int main(void)
{
	char lower;
	char upper;

	for (lower = 'a'; lower <= 'z'; lower++)
	{
		putchar(lower);
	}
	for (upper = 'A'; upper  <= 'Z'; upper++)
	{
		putchar(upper_case);
	}
	putchar ('\n');
	return (0);
}
