#include <stdio.h>
/**
 *main - Entry point
 * Return: End of program
 * (Program by Hayzed)
 */
int main(void)

{
	char ch;

	for (ch = 122; ch >= 97; ch--)
	{
		putchar(alphabet);
	}
	putchar('\n');
	return (0);
}
