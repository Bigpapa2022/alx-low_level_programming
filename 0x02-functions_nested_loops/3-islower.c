#include "main.h"

/**
 * _islower - program that check fpr lower case character
 * @c: contains value to be compared
 * Return: End of program
 * (Code by Hayzed)
 */
int _islower(int c)

{
if (c > 'a' && c < 'z')
{
return (1);
}
else
{
return (0);
}
}
